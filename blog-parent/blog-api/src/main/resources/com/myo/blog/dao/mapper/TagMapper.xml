<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.myo.blog.dao.mapper.TagMapper">

    <select id="findTagsByArticleId" parameterType="java.lang.String" resultType="com.myo.blog.dao.pojo.Tag">
        select id,avatar,tag_name as tagName from myo_tag
        where id in
              (select tag_id from myo_article_tag where article_id=#{articleId})
    </select>

    <select id="findHotsTagIds" parameterType="int" resultType="java.lang.String">
        SELECT tag_id FROM `myo_article_tag` group by tag_id order by count(*) desc limit #{limit}
    </select>

    <select id="findTagsByTagIds" parameterType="list" resultType="com.myo.blog.dao.pojo.Tag">
        select id,tag_name as tagName from myo_tag
        where id in
        <foreach collection="tagIds" item="tagId" separator="," open="(" close=")">
            #{tagId}
        </foreach>
    </select>

</mapper>